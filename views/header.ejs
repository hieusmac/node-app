<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="/public/style.css" rel="stylesheet">
    <title>My Blog</title>
</head>
<body>
<div class="bg-white dark:bg-gray-900 min-h-screen">
    <header class="absolute inset-x-0 top-0 z-50">
        <!--Navbar-->
        <nav class="flex items-center justify-between p-6">
            <div class="flex lg:flex-1">
                <a href="#" class="text-sm font-semibold text-gray-600 dark:text-white">Logo</a>
            </div>
            <!--Hidden navbar if width less than 512px -->
            <div class="flex lg:hidden">
                <button type="button" command="show-modal" commandfor="mobile-menu" class="inline-flex justify-center items-center p-2.5 text-gray-700 dark:text-gray-300">
                    <span class="sr-only">Main menu</span>
                    <svg viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5" data-slot="icon" aria-hidden="true" class="size-6 fill-none">
                        <path d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5" stroke-linecap="round" stroke-linejoin="round" />
                    </svg>
                </button>
            </div>
            <div class="hidden lg:flex lg:gap-x-12">
                <a href="/" class="text-sm font-semibold text-blue-900 dark:text-amber-200">Home</a>
                <a href="/post" class="text-sm font-semibold text-blue-900 dark:text-amber-200">Pages</a>
                <a href="/about" class="text-sm font-semibold text-blue-900 dark:text-amber-200">About</a>
            </div>
            <div class="hidden lg:flex lg:flex-1 lg:justify-end">
                <span aria-hidden="true">&rarr;</span>
            </div>
        </nav>
    </header>

    <!-- Mobile menu, show/hide based on menu open state. -->
    <dialog id="mobile-menu" aria-modal="true" class="lg:hidden justify-end" >
        <!-- Background backdrop, show/hide based on slide-over state. -->
        <div class="fixed z-51 inset-y-0 right-0 w-xs bg-gray-100 dark:bg-gray-500">
            <div class="flex items-center p-6">
                <div class="flex-1">
                    <a href="#" class="text-sm font-semibold text-gray-600 dark:text-white">Logo</a>
                </div>
                <div class="justify-end p-2.5">
                    <button type="button" command="close" commandfor="mobile-menu" aria-hidden="true">
                        <svg viewBox="0 0 24 24" class="stroke-[1.5] fill-none size-6 stroke-current" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M 6 18 L 18 6 M 6 6 L 18 18" />
                        </svg>
                    </button>
                </div>
            </div>
            <div class="flex flex-col p-6 gap-y-8">
                <a href="/" class="font-semibold text-lg text-blue-900 dark:text-amber-200">Home<span>&rarr;</span></a>
                <a href="/post" class="font-semibold text-lg text-blue-900 dark:text-amber-200">Posts<span>&rarr;</span></a>
                <a href="/about" class="font-semibold text-lg text-blue-900 dark:text-amber-200">About<span>&rarr;</span></a>
            </div>
        </div>
    </dialog>